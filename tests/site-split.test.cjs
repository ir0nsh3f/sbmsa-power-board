const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs');
test('5U moved to separate site; original interface retains three sports and historical source',()=>{const h=fs.readFileSync('site/index.html','utf8');assert.ok(!h.includes('data-sport="5ug"'));assert.ok(!h.includes('data-view="rainbow-unicorns"'));for(const s of ['flag','8u','6u'])assert.ok(h.includes(`data-sport="${s}"`));assert.ok(h.includes("next.divisions=next.divisions.filter(d=>d.sport!=='5ug')"));assert.ok(h.includes('https://ir0nsh3f.github.io/rainbow-unicorns-power-board/'));});
